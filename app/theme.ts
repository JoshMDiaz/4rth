import { createTheme } from '@mui/material/styles'

// You can customize these values to match your design
const theme = createTheme({
  palette: {
    primary: {
      main: '#212121',
    },
    secondary: {
      main: '#37a0a0',
    },
  },
})

export default theme
